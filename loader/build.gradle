apply plugin: 'application'

mainClassName = "loader.Main"

dependencies {
    compile fileTree(dir: 'lib', include: '*.jar')
    compile project(":pebl")

    testCompile group: 'junit', name: 'junit', version: '4.11'
}
